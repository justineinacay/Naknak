# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~

# Local secrets — if you ever move Supabase/PayMongo credentials out of
# the HTML files and into a config file, make sure it lands here first.
.env
.env.local
.env.*.local
*.local.json
naknak_secrets.*

# Logs
*.log
npm-debug.log*

# Node (only relevant if you add tooling/tests later — the app itself
# has no build step and no node_modules dependency to run)
node_modules/

# Misc
*.bak
*.tmp
